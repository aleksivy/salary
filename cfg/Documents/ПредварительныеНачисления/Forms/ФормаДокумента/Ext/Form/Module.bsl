
Процедура ПредварительныеНачисленияДатаНачалаПриИзменении(Элемент)
	// Вставить содержимое обработчика.
КонецПроцедуры

Процедура ЗаполнитьОдинаковуюДатуВКолонке(Кнопка)
	лИмяКолонки = ЭлементыФормы.ПредварительныеНачисления.ТекущаяКолонка.Имя;
	Если Не Лев(лИмяКолонки, 4) = "Дата" Тогда
		Сообщить("Станьте в нужную колонку даты в строку с нужным значением даты !!!");
		Возврат;
	КонецЕсли;
	лДатаНадо = ЭлементыФормы.ПредварительныеНачисления.ТекущаяСтрока[ лИмяКолонки ];
	Если Не ЗначениеЗаполнено(лДатаНадо) Тогда
		Сообщить("Не заполнено значение даты в активной строке !!!");
		Возврат;
	КонецЕсли;
	Для каждого лСтрока Из ПредварительныеНачисления Цикл
		лСтрока[ лИмяКолонки ] = лДатаНадо;
	КонецЦикла;
КонецПроцедуры

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	ОсновныеНачисления.Очистить();
КонецПроцедуры
