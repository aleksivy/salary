
Процедура УстановитьМаксимальноеЗначение(ПутьКДанным) Экспорт
	
	Если МаксимальноеЗначение <> 0 Тогда
		ЭлементыФормы.Индикатор.МаксимальноеЗначение = МаксимальноеЗначение;
	КонецЕсли;

КонецПроцедуры

Процедура СформироватьЗаголовок(ПутьКДанным = "")
	
	Если ЗначениеЗаполнено(НаименованиеОбработкиДанных) Тогда
		Заголовок = НаименованиеОбработкиДанных;
	КонецЕсли;
	
КонецПроцедуры

Процедура УстановитьВидимостьПриИзмененииКомментарийДействия(ПутьКДанным)
	
	ЭлементыФормы.КомментарийДействия.Видимость = НЕ ПустаяСтрока(СокрЛП(КомментарийОбработкиДанных));
	
КонецПроцедуры

Процедура УстановитьТекстОшибки(ПутьКДанным)
	
	ЭлементыФормы.ПолеОшибок.УстановитьТекст(Ошибки);
	
КонецПроцедуры

Процедура ЗадачиПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	
	ОформлениеСтроки.Ячейки.Состояние.ОтображатьКартинку = Истина;
	Если ДанныеСтроки.Состояние = Перечисления.СостоянияСобытий.Запланировано Тогда
		ОформлениеСтроки.Ячейки.Состояние.ИндексКартинки = 0;
	ИначеЕсли ДанныеСтроки.Состояние = Перечисления.СостоянияСобытий.Завершено Тогда
		ОформлениеСтроки.Ячейки.Состояние.ИндексКартинки = 1;
	ИначеЕсли ДанныеСтроки.Состояние = Перечисления.СостоянияСобытий.Отменено Тогда
		ОформлениеСтроки.Ячейки.Состояние.ИндексКартинки = 2;
	КонецЕсли; 
	
КонецПроцедуры

ПодключитьОбработчикИзмененияДанных("КомментарийОбработкиДанных",  "УстановитьВидимостьПриИзмененииКомментарийДействия");

ПодключитьОбработчикИзмененияДанных("НаименованиеОбработкиДанных", "СформироватьЗаголовок");

ПодключитьОбработчикИзмененияДанных("МаксимальноеЗначение", "УстановитьМаксимальноеЗначение");

ПодключитьОбработчикИзмененияДанных("Ошибки", "УстановитьТекстОшибки");



